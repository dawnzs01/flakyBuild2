name: Check broken Markdown links

# runs every monday at 9 am
on:
  push:
    branches: [ book-project ]



jobs:
  markdown-link-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: gaurav-nelson/github-action-markdown-link-check@v1
